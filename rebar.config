{cover_enabled, true}.
{cover_excl_mods, [jsonpath_lex, jpparse]}.

{ct_cover, true}.
{ct_verbose, true}.

{deps, [
    {cowboy, "2.8.0"}
]}.

{edoc_opts, [{preprocess, true}]}.

{eunit_opts, [
    {skip_deps, true},
    verbose
]}.
